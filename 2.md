# 字幕样式同步修复记录

## 修复概述

本次修复解决了字幕样式面板修改后无法正确同步到视频字幕显示的问题，核心原因是属性名不匹配。

---

## 变更记录

### 第1次变更：CSSSubtitleRenderer.tsx（完整替换）

**目的**：统一使用 `types.ts` 中定义的属性名，修复样式属性映射错误。

**关键修改点**：

| 修复前（错误属性名） | 修复后（正确属性名） | 说明 |
|---------------------|---------------------|------|
| `fontFamily` | `fontname` | 字体名称属性 |
| `fontSize` | `fontsize` | 字号属性 |
| `color` | `color1` | 主文字颜色 |
| `outlineColor` | `color3` | 描边颜色 |
| `outlineWidth` | `outline` | 描边宽度 |
| `shadowColor` | `color4` | 阴影颜色 |
| `shadowWidth` | `shadow` | 阴影宽度 |
| `marginV` | `margin_v` | 垂直边距 |

**新增特性**：
- 使用多层 `text-shadow` 模拟更平滑的描边效果
- 支持 `offsetY` 垂直偏移属性
- 支持 `margin_l`/`margin_r` 左右边距

---

### 第2次变更：SubtitleStylePanel.tsx（完整替换）

**目的**：使用正确的属性名与 `types.ts` 保持一致，更新UI控件以支持所有样式属性。

**新增控件**：
1. **字体选择** - 支持8种常用字体（Arial、微软雅黑、宋体、黑体等）
2. **字号滑块** - 12-120px范围调节
3. **颜色选择器** - 主文字颜色、描边颜色、阴影颜色（带十六进制输入）
4. **描边宽度滑块** - 0-10px范围
5. **阴影宽度滑块** - 0-10px范围
6. **粗体/斜体/下划线复选框**
7. **边距输入框** - 左边距、右边距、垂直边距、垂直偏移

**接口变更**：
- 原接口：`onEnglishStyleChange` / `onChineseStyleChange`
- 新接口：`updateStyle(lang: 'english' | 'chinese', updates: Partial<SubtitleStyle>)`

---

### 第3次变更：SentenceEditorModal.tsx（局部修改）

**目的**：适配新的组件接口，简化样式传递逻辑。

**修改内容**：

**修改1** - 简化 CSSSubtitleRenderer 调用：
```diff
- <CSSSubtitleRenderer
-   englishStyle={{
-     fontFamily: enStyle.fontname,
-     fontSize: enStyle.fontsize,
-     // ... 手动映射
-   }}
-   chineseStyle={{ ... }}
- />
+ <CSSSubtitleRenderer
+   englishStyle={enStyle}
+   chineseStyle={cnStyle}
+ />
```

**修改2** - 更新 SubtitleStylePanel 调用方式：
```diff
- <SubtitleStylePanel
-   onEnglishStyleChange={setEnStyle}
-   onChineseStyleChange={setCnStyle}
-   onClose={() => setShowStylePanel(false)}
- />
+ <SubtitleStylePanel
+   updateStyle={(lang, updates) => {
+     if (lang === 'english') {
+       setEnStyle((prev) => ({ ...prev, ...updates }));
+     } else {
+       setCnStyle((prev) => ({ ...prev, ...updates }));
+     }
+   }}
+ />
```

---

## 修复的问题

### 问题1：样式属性名不匹配
**根因**：`CSSSubtitleRenderer` 和 `SubtitleStylePanel` 定义了自己的 `SubtitleStyle` 接口，与 `types.ts` 中的定义不一致。

**影响**：修改样式面板中的设置后，字幕显示没有变化。

### 问题2：状态更新方式
**根因**：原实现使用直接赋值方式更新状态，可能导致状态不同步。

**修复**：使用函数式 `setState` 确保状态正确更新。

---

## 测试验证

### 测试步骤：
1. 打开字幕弹窗
2. 点击"样式"按钮打开样式面板
3. 修改字体、字号、颜色等属性
4. 观察视频上的字幕是否实时更新
5. 关闭弹窗后重新打开，验证样式是否保持

### 预期结果：
- ✅ 字体修改生效
- ✅ 字号修改生效  
- ✅ 颜色修改生效
- ✅ 描边/阴影修改生效
- ✅ 粗体/斜体/下划线生效
- ✅ 边距和偏移修改生效

---

# 第4次变更：VideoPlayer.tsx - 优化阴影和描边实现

**目的**：优化主视频字幕渲染性能，统一使用 `-webkit-text-stroke` 实现描边，并增加位置控制属性支持。

**关键修改点**：

| 修改内容 | 说明 |
|----------|------|
| 阴影简化 | 将多层 text-shadow 描边改为 `-webkit-text-stroke`，提升渲染性能 |
| 描边后备 | 当没有阴影时，保留4方向基础描边作为降级方案 |
| 位置属性 | 新增 `position: absolute`、`bottom`、`left`、`right`、`textAlign`、`transform` |
| 布局支持 | 支持 `margin_v`、`margin_l`、`margin_r`、`offsetY` 属性 |
| 文字换行 | 添加 `whiteSpace: 'pre-wrap'` 和 `wordWrap: 'break-word'` |

**代码优化**：
```diff
- shadows.push(`${o}px 0 0 ${outlineC}`, `-${o}px 0 0 ${outlineC}`, ...); // 8层描边
+ WebkitTextStroke: outlineW > 0 ? `${outlineW}px ${outlineC}` : undefined; // 使用CSS描边
```

**影响文件**：
- `src/components/VideoPlayer.tsx` - `buildSubtitleCSS` 函数

**测试验证**：
1. 播放视频时字幕应正确显示在底部居中位置
2. 样式面板修改的字体、颜色、描边等应同步到主视频
3. 长字幕应正确换行显示

---

# 第5次变更：SentenceEditorModal.tsx - 修复自动保存导致的状态循环

**目的**：解决弹窗字幕"出现又消失"的问题，移除自动保存 useEffect，改为显式保存。

**问题根因**：
- 自动保存 `useEffect` 在每次 `enStyle`/`cnStyle` 变化时立即调用 `onUpdate`
- 触发 `App.tsx` 状态更新 → `sentences` 数组重新创建 → `sentence` 对象引用变化
- 导致组件重新渲染或弹窗关闭

**关键修改点**：

| 修改内容 | 说明 |
|----------|------|
| 移除自动保存 | 删除 `useEffect(() => { onUpdate(...) }, [enStyle, cnStyle])` |
| 显式保存函数 | 新增 `saveStyles` 回调函数，只在需要时调用 |
| 关闭按钮 | 点击关闭时先保存样式再关闭弹窗 |
| 提交按钮 | 提交时同时保存文本和样式 |

**代码变更**：
```diff
- // Save style changes via useEffect to avoid rendering issues
- useEffect(() => {
-   onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
- }, [sentence.id, enStyle, cnStyle, onUpdate]);
+ // 显式保存样式：只在点击保存按钮时调用，避免自动保存导致状态循环
+ const saveStyles = useCallback(() => {
+   onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
+ }, [sentence.id, enStyle, cnStyle, onUpdate]);
```

**按钮绑定**：
```diff
- <button className="modal-close-btn" onClick={onClose}>✕</button>
+ <button className="modal-close-btn" onClick={() => { saveStyles(); onClose(); }}>✕</button>

- <button onClick={() => { saveText(); onSubmitAndNext(); }}>
+ <button onClick={() => { saveText(); saveStyles(); onSubmitAndNext(); }}>
```

**影响文件**：
- `src/components/SentenceEditorModal.tsx`

**测试验证**：
1. 打开弹窗 → 字幕应稳定显示不消失
2. 修改样式 → 预览实时更新（本地状态）
3. 关闭弹窗 → 样式保存到全局状态
4. 重新打开 → 样式保持上次保存的值

---

# 第6次变更：SentenceEditorModal.tsx - 修复主视频消失问题

**目的**：解决"主视频消失，弹窗正常"的问题，通过合并保存和阻断重复初始化来确保数据一致性。

**问题根因**：
- `saveStyles` 只保存 `style`，未保存 `englishText`/`chineseText`
- `saveText` 和 `saveStyles` 独立调用导致两次状态更新，中间状态不一致
- `useEffect` 初始化逻辑可能被重复触发

**关键修改点**：

| 修改内容 | 说明 |
|----------|------|
| 新增 `initDoneRef` | 使用 `useRef` 阻断重复初始化 |
| 修改初始化逻辑 | 只在第一次打开或切换句子时初始化 |
| 新增重置逻辑 | 弹窗关闭时重置初始化标记 |
| 合并保存函数 | 新增 `saveAll` 函数，同时保存文本和样式 |
| 更新按钮绑定 | 关闭按钮和提交按钮都使用 `saveAll` |

**代码变更**：

```diff
+ // 用于阻断重复初始化的 ref
+ const initDoneRef = useRef(false);

- useEffect(() => {
-   setEnglishText(sentence.englishText);
-   setChineseText(sentence.chineseText);
-   setEnStyle({ ...sentence.style.english });
-   setCnStyle({ ...sentence.style.chinese });
- }, [sentence.id]);
+ useEffect(() => {
+   // 只在第一次打开或切换句子时初始化
+   if (!initDoneRef.current) {
+     initDoneRef.current = true;
+     setEnglishText(sentence.englishText);
+     setChineseText(sentence.chineseText);
+     setEnStyle({ ...sentence.style.english });
+     setCnStyle({ ...sentence.style.chinese });
+   }
+ }, [sentence.id]);

+ // 重置初始化标记（弹窗关闭时）
+ useEffect(() => {
+   if (!playing && !showStylePanel) {
+     initDoneRef.current = false;
+   }
+ }, [playing, showStylePanel]);

- const saveStyles = useCallback(() => {
-   onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
- }, [sentence.id, enStyle, cnStyle, onUpdate]);
+ const saveAll = useCallback(() => {
+   const updates: Partial<Sentence> = {
+     style: { english: enStyle, chinese: cnStyle },
+   };
+   if (englishText !== sentence.englishText) updates.englishText = englishText;
+   if (chineseText !== sentence.chineseText) updates.chineseText = chineseText;
+   if (Object.keys(updates).length > 0) onUpdate(sentence.id, updates);
+ }, [englishText, chineseText, enStyle, cnStyle, sentence.id, sentence.englishText, sentence.chineseText, onUpdate]);

- <button onClick={() => { saveStyles(); onClose(); }}>✕</button>
+ <button onClick={() => { saveAll(); onClose(); }}>✕</button>

- <button onClick={() => { saveText(); saveStyles(); onSubmitAndNext(); }}>
+ <button onClick={() => { saveAll(); onSubmitAndNext(); }}>
```

**影响文件**：
- `src/components/SentenceEditorModal.tsx`

**测试验证**：
1. 打开弹窗 → 字幕稳定显示
2. 修改样式/文本 → 预览实时更新
3. 关闭弹窗 → 所有数据（文本+样式）保存到全局状态
4. 主视频字幕应正常显示，不再消失

---

# 第7次变更：VideoPlayer.tsx + CSSSubtitleRenderer.tsx - 修复主界面字幕不显示问题

**目的**：解决"主界面视频看不到字幕，但弹窗编辑时可以看到字幕"的问题，统一字幕渲染逻辑并修复容器尺寸初始化问题。

**问题根因**：
1. **渲染逻辑不统一**：主界面使用 `buildSubtitleCSS` 函数，弹窗使用 `CSSSubtitleRenderer` 组件
2. **容器尺寸初始化问题**：`videoRenderRect` 初始值为 `{ x: 0, y: 0, width: 0, height: 0 }`，导致字幕容器尺寸为零
3. **CSSSubtitleRenderer 固定尺寸问题**：使用固定的 `width: 1280px` 和 `height: 720px`，无法自适应父容器

**关键修改点**：

| 修改内容 | 说明 |
|----------|------|
| 统一渲染组件 | VideoPlayer.tsx 改用 `CSSSubtitleRenderer` 组件 |
| 移除重复代码 | 删除 VideoPlayer.tsx 中的 `buildSubtitleCSS` 函数 |
| 修复容器尺寸 | 字幕容器使用 `width: videoRenderRect.width || "100%"` 作为后备值 |
| 自适应布局 | CSSSubtitleRenderer 改用绝对定位（`top/left/right/bottom: 0`） |

**代码变更 - VideoPlayer.tsx**：

```diff
- import type { Sentence, SubtitleStyle } from "../types";

- function buildSubtitleCSS(style: SubtitleStyle): React.CSSProperties {
-   const outlineW = style.outline ?? 2;
-   const outlineC = style.color3 ?? "#000000";
-   const shadowW = style.shadow ?? 1;
-   const shadowC = style.color4 ?? "#000000";
-   // ... 48行代码
- }
+ import type { Sentence } from "../types";
+ import CSSSubtitleRenderer from "./CSSSubtitleRenderer";
```

```diff
- {currentSentence && (
-   <div className="video-subtitles" style={{ ... }}>
-     {currentSentence.chineseText && currentSentence.style?.chinese && (
-       <div style={{ ...buildSubtitleCSS(currentSentence.style.chinese) }}>
-         {currentSentence.chineseText}
-       </div>
-     )}
-     {currentSentence.englishText && currentSentence.style?.english && (
-       <div style={{ ...buildSubtitleCSS(currentSentence.style.english) }}>
-         {currentSentence.englishText}
-       </div>
-     )}
-   </div>
- )}
+ {currentSentence && currentSentence.style?.chinese && currentSentence.style?.english && (
+   <div className="video-subtitles" style={{
+     position: "absolute",
+     left: videoRenderRect.x,
+     top: videoRenderRect.y,
+     width: videoRenderRect.width || "100%",
+     height: videoRenderRect.height || "100%",
+     pointerEvents: "none",
+     zIndex: 10,
+   }}>
+     <CSSSubtitleRenderer
+       englishText={currentSentence.englishText || ""}
+       chineseText={currentSentence.chineseText || ""}
+       englishStyle={currentSentence.style.english}
+       chineseStyle={currentSentence.style.chinese}
+     />
+   </div>
+ )}
```

**代码变更 - CSSSubtitleRenderer.tsx**：

```diff
- const CSSSubtitleRenderer: React.FC<CSSSubtitleRendererProps> = ({
-   englishText,
-   chineseText,
-   englishStyle,
-   chineseStyle,
-   width = 1280,
-   height = 720,
- }) => {
-   return (
-     <div style={{
-       position: 'relative',
-       width: `${width}px`,
-       height: `${height}px`,
-       // ...
-     }}>
+ const CSSSubtitleRenderer: React.FC<CSSSubtitleRendererProps> = ({
+   englishText,
-   chineseText,
-   englishStyle,
-   chineseStyle,
- }) => {
-   return (
-     <div style={{
-       position: 'absolute',
-       top: 0,
-       left: 0,
-       right: 0,
-       bottom: 0,
-       // ...
-     }}>
```

**代码变更 - SentenceEditorModal.css**：

```diff
+ /* CSS Subtitle Renderer */
+ .css-subtitle-renderer {
+   position: absolute;
+   top: 0;
+   left: 0;
+   right: 0;
+   bottom: 0;
+   pointer-events: none;
+   z-index: 10;
+ }
+
+ .css-subtitle-renderer .subtitle-line {
+   text-align: center;
+   white-space: pre-wrap;
+   word-break: break-word;
+ }
```

**影响文件**：
- `src/components/VideoPlayer.tsx` - 统一使用 CSSSubtitleRenderer
- `src/components/CSSSubtitleRenderer.tsx` - 改为自适应布局
- `src/components/SentenceEditorModal.css` - 添加样式定义

**测试验证**：
1. 主界面视频播放 → 字幕应正常显示
2. 打开弹窗编辑 → 字幕应正常显示
3. 修改样式面板 → 主界面和弹窗样式应同步
4. 关闭弹窗 → 主界面字幕保持正常显示

---

## 第 8 次变更：样式面板改为 Tab 页

**目的**：将中英文样式编辑器改为 Tab 页形式，避免同时显示导致界面过长
**修复内容**：
1. 添加 `useState` 管理 `activeTab` 状态（'english' | 'chinese'）
2. 新增 Tab 切换按钮，支持中英文切换
3. 只显示当前选中的 Tab 对应的样式编辑器
4. 保持原有的样式编辑功能不变

**代码变更**：

```diff
import React from 'react';
+ import React, { useState } from 'react';
import type { SubtitleStyle } from '../types';

// ...

const SubtitleStylePanel: React.FC<SubtitleStylePanelProps> = ({
  englishStyle,
  chineseStyle,
  updateStyle,
}) => {
+  const [activeTab, setActiveTab] = useState<'english' | 'chinese'>('english');

  return (
    <div ...>
+      {/* Tab 切换 */}
+      <div style={{ display: 'flex', gap: 8, marginBottom: 20, borderBottom: '1px solid #e5e7eb' }}>
+        <button ... onClick={() => setActiveTab('english')}>英文字幕 (English)</button>
+        <button ... onClick={() => setActiveTab('chinese')}>中文字幕 (Chinese)</button>
+      </div>

+      {/* 样式编辑器 */}
+      {activeTab === 'english' ? (
        <StyleEditor
          title="英文字幕样式 (English)"
          style={englishStyle}
          onUpdate={(updates) => updateStyle('english', updates)}
        />
+      ) : (
        <StyleEditor
          title="中文字幕样式 (Chinese)"
          style={chineseStyle}
          onUpdate={(updates) => updateStyle('chinese', updates)}
        />
+      )}
    </div>
  );
};
```

**影响文件**：
- `src/components/SubtitleStylePanel.tsx`

**测试验证**：
1. 打开样式面板 → 显示"英文字幕" Tab
2. 点击"中文字幕" Tab → 切换到中文样式编辑器
3. 在英文 Tab 修改样式 → 英文预览更新
4. 在中文 Tab 修改样式 → 中文预览更新
5. 切换 Tab → 样式保持不变
