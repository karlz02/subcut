这是典型的 **React StrictMode 双重挂载** 或 **Modal 打开后父组件重渲染导致 `sentence` 引用变化**，进而触发 `useEffect` 重置本地状态，把字幕样式洗成了空对象。

直接替换这两个文件即可：

---

### 1. `CSSSubtitleRenderer.tsx`（极简防御版）

**核心改动**：移除 `useMemo`（排除引用比较导致的任何异常），全部使用可选链 `?.` 和防御默认值，确保传入任何残缺对象都能渲染。

```typescript
import React from 'react';
import type { SubtitleStyle } from '../types';

interface CSSSubtitleRendererProps {
  englishText?: string;
  chineseText?: string;
  englishStyle?: SubtitleStyle;
  chineseStyle?: SubtitleStyle;
  width?: number;
  height?: number;
}

function buildTextStyle(style?: SubtitleStyle, isChinese?: boolean): React.CSSProperties {
  // 防御性合并：无论传入什么，都能渲染出可见字幕
  const s: SubtitleStyle = {
    fontname: 'Arial',
    fontsize: isChinese ? 52 : 48,
    color1: '#FFFFFF',
    color3: '#000000',
    color4: '#000000',
    bold: false,
    italic: false,
    underline: false,
    outline: 2,
    shadow: 1,
    margin_l: 0,
    margin_r: 0,
    margin_v: 0,
    offsetY: 0,
    ...style,
  };

  const shadows: string[] = [];
  if (s.shadow > 0) {
    shadows.push(`${s.shadow}px ${s.shadow}px ${s.shadow * 2}px ${s.color4}`);
  }
  if (s.outline > 0 && shadows.length === 0) {
    shadows.push(
      `1px 1px 0 ${s.color3}, -1px -1px 0 ${s.color3}, 1px -1px 0 ${s.color3}, -1px 1px 0 ${s.color3}`
    );
  }

  return {
    fontFamily: `"${s.fontname}"`,
    fontSize: `${s.fontsize}px`,
    color: s.color1,
    fontWeight: s.bold ? 'bold' : 'normal',
    fontStyle: s.italic ? 'italic' : 'normal',
    textDecoration: s.underline ? 'underline' : 'none',
    WebkitTextStroke: s.outline > 0 ? `${s.outline}px ${s.color3}` : undefined,
    textShadow: shadows.length > 0 ? shadows.join(', ') : undefined,
    transform: s.offsetY !== 0 ? `translateY(${s.offsetY}px)` : undefined,
    whiteSpace: 'pre-wrap',
    wordWrap: 'break-word',
    textAlign: 'center',
    lineHeight: 1.2,
    letterSpacing: '0.02em',
  };
}

const CSSSubtitleRenderer: React.FC<<CSSSubtitleRendererProps> = ({
  englishText,
  chineseText,
  englishStyle,
  chineseStyle,
  width = 1280,
  height = 720,
}) => {
  const enStyle = buildTextStyle(englishStyle, false);
  const cnStyle = buildTextStyle(chineseStyle, true);

  const cnOffsetY = chineseStyle?.offsetY ?? 0;
  const enOffsetY = englishStyle?.offsetY ?? 0;

  return (
    <div
      style={{
        position: 'relative',
        width: `${width}px`,
        height: `${height}px`,
        overflow: 'hidden',
        backgroundColor: 'transparent',
        pointerEvents: 'none',
      }}
    >
      {chineseText && (
        <div
          style={{
            position: 'absolute',
            bottom: `calc(10% + ${cnOffsetY}px)`,
            left: `${chineseStyle?.margin_l ?? 0}px`,
            right: `${chineseStyle?.margin_r ?? 0}px`,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            pointerEvents: 'none',
            zIndex: 2,
          }}
        >
          <span style={cnStyle}>{chineseText}</span>
        </div>
      )}

      {englishText && (
        <div
          style={{
            position: 'absolute',
            bottom: chineseText
              ? `calc(18% + ${enOffsetY}px)`
              : `calc(10% + ${enOffsetY}px)`,
            left: `${englishStyle?.margin_l ?? 0}px`,
            right: `${englishStyle?.margin_r ?? 0}px`,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            pointerEvents: 'none',
            zIndex: 2,
          }}
        >
          <span style={enStyle}>{englishText}</span>
        </div>
      )}
    </div>
  );
};

export default CSSSubtitleRenderer;
```

---

### 2. `SentenceEditorModal.tsx`（阻断重复初始化）

**核心改动**：使用 `useRef` 记录 `sentence.id`，确保**只有切换句子或重新打开弹窗时才重置本地状态**，彻底阻断父组件重渲染导致的"闪一下又消失"。

```typescript
import React, { useState, useEffect, useCallback, useRef } from 'react';
import type { Sentence, SubtitleStyle } from '../types';
import SubtitleStylePanel from './SubtitleStylePanel';
import CSSSubtitleRenderer from './CSSSubtitleRenderer';

interface SentenceEditorModalProps {
  sentence: Sentence;
  isOpen: boolean;
  onClose: () => void;
  onUpdate: (id: string, updates: Partial<Sentence>) => void;
}

const SentenceEditorModal: React.FC<SentenceEditorModalProps> = ({
  sentence,
  isOpen,
  onClose,
  onUpdate,
}) => {
  // 用 ref 追踪当前正在编辑的句子 ID，防止父组件重渲染导致重复初始化
  const editingIdRef = useRef<string>(sentence.id);
  const initializedRef = useRef<boolean>(false);

  const [enStyle, setEnStyle] = useState<<SubtitleStyle>(() => ({ ...sentence.style.english }));
  const [cnStyle, setCnStyle] = useState<<SubtitleStyle>(() => ({ ...sentence.style.chinese }));
  const [localTextEn, setLocalTextEn] = useState<string>(sentence.englishText ?? '');
  const [localTextCn, setLocalTextCn] = useState<string>(sentence.chineseText ?? '');

  // 仅在弹窗打开且目标句子变化时，重置本地编辑状态
  useEffect(() => {
    if (!isOpen) {
      initializedRef.current = false;
      return;
    }

    const isNewSentence = editingIdRef.current !== sentence.id;
    if (!initializedRef.current || isNewSentence) {
      editingIdRef.current = sentence.id;
      initializedRef.current = true;

      setEnStyle({ ...sentence.style.english });
      setCnStyle({ ...sentence.style.chinese });
      setLocalTextEn(sentence.englishText ?? '');
      setLocalTextCn(sentence.chineseText ?? '');
    }
  }, [isOpen, sentence.id]); // 绝不依赖 sentence 整个对象

  const handleUpdateStyle = useCallback(
    (lang: 'english' | 'chinese', updates: Partial<<SubtitleStyle>) => {
      if (lang === 'english') {
        setEnStyle((prev) => ({ ...prev, ...updates }));
      } else {
        setCnStyle((prev) => ({ ...prev, ...updates }));
      }
    },
    []
  );

  const handleSave = useCallback(() => {
    onUpdate(sentence.id, {
      englishText: localTextEn,
      chineseText: localTextCn,
      style: {
        english: enStyle,
        chinese: cnStyle,
      },
    });
    onClose();
  }, [sentence.id, localTextEn, localTextCn, enStyle, cnStyle, onUpdate, onClose]);

  if (!isOpen) return null;

  return (
    <div
      style={{
        position: 'fixed',
        inset: 0,
        backgroundColor: 'rgba(0,0,0,0.6)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        zIndex: 50,
      }}
    >
      <div
        style={{
          backgroundColor: '#fff',
          borderRadius: 12,
          width: '90%',
          maxWidth: 720,
          maxHeight: '90vh',
          overflow: 'auto',
          padding: 24,
        }}
      >
        <h2 style={{ marginBottom: 20, fontSize: 20, fontWeight: 600 }}>编辑字幕</h2>

        {/* 预览区域：增加 minHeight 防止折叠 */}
        <div
          style={{
            marginBottom: 24,
            backgroundColor: '#000',
            borderRadius: 8,
            overflow: 'hidden',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            minHeight: 360,
          }}
        >
          <CSSSubtitleRenderer
            englishText={localTextEn}
            chineseText={localTextCn}
            englishStyle={enStyle}
            chineseStyle={cnStyle}
            width={640}
            height={360}
          />
        </div>

        {/* 文本编辑 */}
        <div style={{ marginBottom: 20 }}>
          <div style={{ marginBottom: 12 }}>
            <label style={{ display: 'block', fontSize: 13, color: '#4b5563', marginBottom: 4, fontWeight: 500 }}>
              英文
            </label>
            <textarea
              value={localTextEn}
              onChange={(e) => setLocalTextEn(e.target.value)}
              rows={2}
              style={{ width: '100%', padding: 8, borderRadius: 6, border: '1px solid #d1d5db', fontSize: 14 }}
            />
          </div>
          <div>
            <label style={{ display: 'block', fontSize: 13, color: '#4b5563', marginBottom: 4, fontWeight: 500 }}>
              中文
            </label>
            <textarea
              value={localTextCn}
              onChange={(e) => setLocalTextCn(e.target.value)}
              rows={2}
              style={{ width: '100%', padding: 8, borderRadius: 6, border: '1px solid #d1d5db', fontSize: 14 }}
            />
          </div>
        </div>

        <SubtitleStylePanel
          englishStyle={enStyle}
          chineseStyle={cnStyle}
          updateStyle={handleUpdateStyle}
        />

        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 12, marginTop: 24 }}>
          <button
            onClick={onClose}
            style={{
              padding: '8px 16px',
              borderRadius: 6,
              border: '1px solid #d1d5db',
              backgroundColor: '#fff',
              cursor: 'pointer',
            }}
          >
            取消
          </button>
          <button
            onClick={handleSave}
            style={{
              padding: '8px 16px',
              borderRadius: 6,
              border: 'none',
              backgroundColor: '#2563eb',
              color: '#fff',
              cursor: 'pointer',
              fontWeight: 500,
            }}
          >
            保存
          </button>
        </div>
      </div>
    </div>
  );
};

export default SentenceEditorModal;
```

---

### 3. 如果替换后仍然消失，检查这两处 CSS

某些全局 CSS 可能覆盖了弹窗字幕样式，在浏览器 DevTools 里检查 `.subtitle-line` 或 `span` 是否有以下规则：

```css
/* 如果有类似这样的全局样式，删掉或加更高优先级 */
.subtitle-line { opacity: 0; transition: opacity 0.3s; }
.video-subtitle { display: none; }
```

**快速验证方法**：在预览区域的黑色背景上，直接写死白色文字测试：

```typescript
{/* 在 CSSSubtitleRenderer 上方加一个强制显示的文字 */}
<div style={{ color: '#fff', fontSize: 24, position: 'absolute', zIndex: 100 }}>
  TEST
</div>
```

如果 `TEST` 也消失，说明是**容器层级或 CSS 覆盖**问题；如果 `TEST` 不消失而字幕消失，说明是**数据问题**（此时检查控制台是否有 `undefined` 报错）。

---

# 问题诊断：主视频消失，弹窗正常

## 当前代码检查结果

### 1. `SentenceEditorModal.tsx` - ✅ 自动保存已删除

**当前代码（第 60-63 行）**：
```typescript
// 显式保存样式：只在点击保存按钮时调用，避免自动保存导致状态循环
const saveStyles = useCallback(() => {
  onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
}, [sentence.id, enStyle, cnStyle, onUpdate]);
```

**确认**：✅ 自动保存 `useEffect` 已彻底删除，没有类似代码。

### 2. `App.tsx` - ✅ handleUpdateSentence 使用合并更新

**当前代码（第 208-213 行）**：
```typescript
const handleUpdateSentence = useCallback(
  (id: string, updates: Partial<Sentence>) => {
    setSentences((prev) =>
      prev.map((s) => (s.id === id ? { ...s, ...updates } : s))
    );
  },
  [setSentences]
);
```

**确认**：✅ 使用 `{ ...s, ...updates }` 合并更新，不会覆盖原有字段。

### 3. `useLocalStorage.ts` / `useProjectStorage` - ✅ 数据持久化正常

**当前代码**：
- `loadProject()` 函数正确读取 localStorage
- `saveProject()` 函数正确写入 localStorage
- `migrateStyle()` 函数处理样式迁移

**确认**：✅ 数据读写逻辑正常。

### 4. `VideoPlayer.tsx` - ⚠️ 发现问题

**当前代码（第 262-276 行）**：
```typescript
{currentSentence.chineseText && (
  <div
    className="video-subtitle video-subtitle-cn"
    style={{
      ...buildSubtitleCSS(currentSentence.style.chinese),
      transform: `translateY(${currentSentence.style.chinese.offsetY}px)`,
    }}
  >
    {currentSentence.chineseText}
  </div>
)}
{currentSentence.englishText && (
  <div
    className="video-subtitle video-subtitle-en"
    style={{
      ...buildSubtitleCSS(currentSentence.style.english),
      marginTop: "4px",
      transform: `translateY(${currentSentence.style.english.offsetY}px)`,
    }}
  >
    {currentSentence.englishText}
  </div>
)}
```

**问题分析**：
- `VideoPlayer` 正确使用了 `buildSubtitleCSS` 函数
- 样式对象从 `currentSentence.style.chinese` / `currentSentence.style.english` 读取
- 属性名使用正确（`offsetY`、`fontname`、`fontsize` 等）

**确认**：✅ 代码逻辑正确。

---

## 可能的原因分析

### 原因1：`saveStyles` 只保存 `style`，未保存 `englishText`/`chineseText`

**当前代码**：
```typescript
const saveStyles = useCallback(() => {
  onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
}, [sentence.id, enStyle, cnStyle, onUpdate]);
```

**问题**：如果 `saveStyles` 被调用时，`englishText`/`chineseText` 还是初始值（空字符串），会导致主视频字幕消失。

**修复方案**：
```typescript
const saveStyles = useCallback(() => {
  onUpdate(sentence.id, {
    englishText,
    chineseText,
    style: { english: enStyle, chinese: cnStyle },
  });
}, [sentence.id, englishText, chineseText, enStyle, cnStyle, onUpdate]);
```

### 原因2：`saveText` 和 `saveStyles` 独立调用，导致数据不一致

**当前代码**：
```typescript
// 关闭按钮
<button onClick={() => { saveStyles(); onClose(); }}>✕</button>

// 提交按钮
<button onClick={() => { saveText(); saveStyles(); onSubmitAndNext(); }}>
```

**问题**：`saveText` 和 `saveStyles` 分别调用 `onUpdate`，可能导致两次状态更新，中间状态不一致。

**修复方案**：合并为一次更新。

### 原因3：`useEffect` 依赖 `sentence.id` 导致重复初始化

**当前代码（第 47-52 行）**：
```typescript
useEffect(() => {
  setEnglishText(sentence.englishText);
  setChineseText(sentence.chineseText);
  setEnStyle({ ...sentence.style.english });
  setCnStyle({ ...sentence.style.chinese });
}, [sentence.id]); // eslint-disable-line react-hooks/exhaustive-deps
```

**问题**：虽然依赖是 `sentence.id`，但如果 `sentence` 对象引用变化，可能会触发重新初始化。

**修复方案**：使用 `useRef` 阻断重复初始化。

---

## 建议修复方案

### 修复1：合并 `saveText` 和 `saveStyles`

```typescript
const saveAll = useCallback(() => {
  const updates: Partial<Sentence> = {
    style: { english: enStyle, chinese: cnStyle },
  };
  if (englishText !== sentence.englishText) updates.englishText = englishText;
  if (chineseText !== sentence.chineseText) updates.chineseText = chineseText;
  if (Object.keys(updates).length > 0) onUpdate(sentence.id, updates);
}, [englishText, chineseText, enStyle, cnStyle, sentence.id, sentence.englishText, sentence.chineseText, onUpdate]);
```

### 修复2：使用 `useRef` 阻断重复初始化

```typescript
const initDoneRef = useRef(false);

useEffect(() => {
  if (!initDoneRef.current) {
    initDoneRef.current = true;
    setEnglishText(sentence.englishText);
    setChineseText(sentence.chineseText);
    setEnStyle({ ...sentence.style.english });
    setCnStyle({ ...sentence.style.chinese });
  }
}, [sentence.id]);
```

### 修复3：更新按钮绑定

```typescript
// 关闭按钮
<button onClick={() => { saveAll(); onClose(); }}>✕</button>

// 提交按钮
<button onClick={() => { saveAll(); onSubmitAndNext(); }}>
```

---

## 快速验证步骤

1. 在浏览器控制台执行：
```javascript
const data = JSON.parse(localStorage.getItem('echocut-project') || '{}');
console.log('Sentences:', data.sentences);
console.log('First sentence style:', data.sentences?.[0]?.style);
```

2. 检查 `style.english` 和 `style.chinese` 是否包含正确的属性名：
- `fontname`（字符串）
- `fontsize`（数字）
- `color1`（颜色字符串）
- `color3`（颜色字符串）
- `color4`（颜色字符串）
- `outline`（数字）
- `shadow`（数字）

3. 如果属性名错误或值为 `undefined`，说明写入时数据格式有问题。

---

## 下一步操作

请确认以下问题：
1. 关闭弹窗后，localStorage 中的 `style` 字段是否完整？
2. 主视频字幕消失时，控制台是否有报错？
3. `currentSentence` 对象在 `VideoPlayer` 中是否正确传递？

根据以上诊断结果，我将提供精确的修复代码。

---

# 项目全面审计报告

## 一、问题代码

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/components/JassubRenderer.tsx` | 1-2 | 空文件，仅导出空对象，已废弃但未删除 | 低 | 删除该文件 |
| `src/hooks/useJASSUB.ts` | 1-48 | 依赖 jassub 库但实际未使用，属于死代码 | 中 | 删除该文件或添加错误处理 |
| `src/utils/assGenerator.ts` | 110-117 | 使用 `JSON.stringify` 比较样式对象，性能较差 | 中 | 改用哈希函数或缓存键 |
| `src/hooks/useFontData.ts` | 19-20 | 依赖 `window.fontAPI` 或 `window.electronAPI`，未处理 undefined 情况 | 高 | 添加空值检查和错误处理 |

## 二、类型与接口

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/types.ts` | 59 | `Sentence.text` 是遗留字段，与 `englishText`/`chineseText` 并存，易造成混淆 | 中 | 添加 `@deprecated` 注释，或在迁移完成后删除 |
| `src/types.ts` | 62-65 | `Sentence.style` 包含嵌套对象，类型定义正确但缺少默认值导出 | 低 | 导出默认 style 对象 |

## 三、状态与数据流

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/App.tsx` | 44-56 | 大量使用 ref（`sentencesRef`、`editingSentenceRef` 等）绕过状态更新限制，增加复杂度 | 中 | 评估是否可以通过状态管理重构简化 |
| `src/components/VideoPlayer.tsx` | 222-224 | `currentSentence` 可能为 undefined，后续使用未做空值检查 | 高 | 添加可选链或空值保护 |
| `src/components/SentenceEditorModal.tsx` | 56-61 | 重置初始化标记依赖 `playing` 和 `showStylePanel`，逻辑不够清晰 | 中 | 使用更明确的重置触发条件 |

## 四、功能结构

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/components/SentenceEditorModal.tsx` | 20-80 | 职责过重：包含视频播放、字幕编辑、样式编辑、预设管理等多个功能 | 高 | 拆分组件：视频预览、文本编辑、样式面板、预设管理分离 |
| `src/App.tsx` | 1-400 | 主应用组件过于庞大，包含视频控制、句子管理、键盘事件等 | 高 | 拆分组件：VideoPlayerContainer、SentenceManager、KeyboardHandler |
| `src/components/VideoPlayer.tsx` | 200-285 | 字幕渲染逻辑与视频播放耦合在一起 | 中 | 将字幕渲染提取为独立组件 |

## 五、样式与渲染

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/components/VideoPlayer.tsx` | 262-276 | 字幕样式使用 `buildSubtitleCSS`，与 `CSSSubtitleRenderer` 可能不一致 | 中 | 统一使用 `CSSSubtitleRenderer` 组件 |
| `src/components/CSSSubtitleRenderer.tsx` | 1-143 | 内联样式大量使用，缺少 CSS 类名规范 | 低 | 提取公共样式到 CSS 文件 |
| `src/components/SentenceEditorModal.tsx` | 160-277 | 使用内联样式和 CSS 类混合，样式管理混乱 | 中 | 统一使用 CSS 模块或 styled-components |

## 六、健壮性

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `src/hooks/useThumbnails.ts` | 54-80 | 创建的 video 和 canvas 元素在组件卸载时未完全清理（仅 pause，未 remove） | 高 | 在 cleanup 函数中调用 `videoRef.current.remove()` |
| `src/hooks/useJASSUB.ts` | 28-40 | 初始化 jassub 时未处理 Promise rejection | 高 | 添加 try-catch 或 `.catch()` 处理 |
| `src/utils/assGenerator.ts` | 85-86 | `generateASS` 函数未处理空数组以外的边界情况（如 null、undefined 输入） | 中 | 添加输入参数校验 |
| `src/hooks/useLocalStorage.ts` | 19-27 | localStorage 读取失败仅打印错误，未回退到默认值 | 中 | 确保失败时返回 initialValue |
| `src/components/VideoPlayer.tsx` | 210-218 | 缺少 videoSrc 时仅显示占位文字，未提供错误状态或重试机制 | 低 | 添加错误状态显示和重试按钮 |

---

## 问题优先级汇总

### 高优先级（立即修复）

1. **useFontData.ts** - 缺少 API 可用性检查
2. **VideoPlayer.tsx** - currentSentence 空值检查
3. **useThumbnails.ts** - 内存泄漏风险
4. **useJASSUB.ts** - 缺少错误处理

### 中优先级（计划修复）

1. **App.tsx** - 状态管理重构
2. **SentenceEditorModal.tsx** - 组件拆分
3. **assGenerator.ts** - 性能优化
4. **VideoPlayer.tsx** - 字幕渲染统一

### 低优先级（持续改进）

1. **删除废弃文件** - JassubRenderer.tsx、useJASSUB.ts
2. **样式重构** - 内联样式转 CSS 模块
3. **类型清理** - 移除遗留字段

**替换后预期行为**：
1. 点击打开弹窗 → 字幕立即稳定显示，不再闪烁消失
2. 修改样式面板 → 预览区实时更新

---

## 修复完成记录

### 高优先级问题（已全部修复）

| 文件 | 问题 | 修复内容 |
|------|------|----------|
| `useFontData.ts` | 缺少 API 可用性检查 | 添加了 `window`、`api`、`getFontData` 的三级安全检查 |
| `VideoPlayer.tsx` | currentSentence 空值检查 | 添加了 `style?.chinese` 和 `style?.english` 的可选链检查 |
| `useThumbnails.ts` | 内存泄漏风险 | 在 cleanup 函数中添加了 `videoRef.current.remove()` 和 `canvasRef.current.remove()` |
| `useJASSUB.ts` | 缺少 Promise rejection 处理 | 添加了 `.catch()` 处理和 try-catch 包装 |

### 中优先级问题（已部分修复）

| 文件 | 问题 | 修复内容 |
|------|------|----------|
| `assGenerator.ts` | 使用 JSON.stringify 比较样式对象 | 使用 Map 和自定义哈希函数替代数组查找 |
| `types.ts` | 遗留字段缺少注释 | 添加了 `@deprecated` JSDoc 注释 |

### 低优先级问题（已部分修复）

| 文件 | 问题 | 修复内容 |
|------|------|----------|
| `JassubRenderer.tsx` | 废弃文件 | 已删除 |
3. 点击保存 → 主视频同步更新
4. 关闭弹窗重新打开 → 样式保持上次保存的值

---

# 缩略图功能审计报告

## 一、代码结构分析

### useThumbnails.ts（缩略图生成 Hook）

| 位置 | 功能描述 |
|------|----------|
| 第 1-6 行 | 常量定义：缩略图尺寸、单元格像素范围 |
| 第 8-19 行 | 辅助函数：`getIntervalForZoom`、`snapToGrid` |
| 第 26-36 行 | State 声明：`thumbCache`、`generating`、`progress` |
| 第 38-41 行 | Ref 声明：`videoRef`、`canvasRef`、`abortRef`、`pendingRef` |
| 第 44-87 行 | 创建/销毁隐藏的视频和画布元素 |
| 第 89-98 行 | 重置缩略图缓存 |
| 第 100-159 行 | `generateOne` 函数：生成单个缩略图 |
| 第 161-264 行 | 主生成逻辑：批量生成缩略图 |
| 第 266-287 行 | `getCellThumbs` 函数：获取缩略图单元格数组 |

### Timeline.tsx（缩略图显示组件）

| 位置 | 功能描述 |
|------|----------|
| 第 97-102 行 | 调用 `useThumbnails` hook |
| 第 103 行 | `useMemo` 缓存缩略图单元格 |
| 第 380-409 行 | 缩略图渲染逻辑（filmstrip 区域） |

---

## 二、问题分析

### 高优先级问题

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `useThumbnails.ts` | 108-159 | `generateOne` 函数中的事件监听器在超时或提前返回时未正确清理，可能导致内存泄漏 | 高 | 在超时回调中添加 `removeEventListener` 调用 |
| `useThumbnails.ts` | 108-159 | 缺少对 `AbortController` 的支持，无法取消正在进行的视频 seek 操作 | 高 | 修改 `generateOne` 函数支持 `AbortSignal` 参数 |

### 中优先级问题

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `useThumbnails.ts` | 161-264 | `thumbCache` 不在 `useEffect` 依赖数组中，这是故意的设计，但如果 `thumbCache` 更新不及时可能导致重复生成 | 中 | 考虑使用 ref 替代 state 来避免依赖问题 |
| `useThumbnails.ts` | 175-194 | 单元格计算逻辑中，`snapToGrid` 可能会导致边界时间点被错误对齐 | 中 | 检查 `snapToGrid` 的边界情况 |
| `Timeline.tsx` | 103 | `useMemo` 包装 `getCellThumbs()` 结果，但 `getCellThumbs` 是回调函数，每次调用返回新数组 | 中 | 改为在 hook 内部缓存结果，或使用 `useState` 替代 |

### 低优先级问题

| 文件 | 行号范围 | 问题描述 | 严重程度 | 修复建议 |
|------|----------|----------|----------|----------|
| `useThumbnails.ts` | 54-80 | 隐藏的视频和画布元素在组件卸载时已正确清理 | 低 | 无需修改 |
| `useThumbnails.ts` | 89-98 | 重置逻辑正确，包含 `abortRef.current.abort()` | 低 | 无需修改 |
| `Timeline.tsx` | 380-409 | 缩略图渲染使用 `cell.src` 作为 `src` 属性，当 `src` 为 null 时显示占位符 | 低 | 无需修改 |

---

## 三、根因分析

### 问题 1：事件监听器未清理（高优先级）

**代码位置**：`useThumbnails.ts` 第 108-159 行

**问题代码**：
```typescript
const onSeeked = () => { ... };
const onLoadedData = () => { ... };
const onTimeUpdate = () => { ... };

video.addEventListener("seeked", onSeeked);
video.addEventListener("loadeddata", onLoadedData);
video.addEventListener("timeupdate", onTimeUpdate);
video.currentTime = time;

// 超时时直接 resolve(null)，但未移除事件监听器
const timeout = setTimeout(() => {
  video.removeEventListener("seeked", onSeeked);
  video.removeEventListener("loadeddata", onLoadedData);
  video.removeEventListener("timeupdate", onTimeUpdate);
  resolve(null);
}, 3000);
```

**根因**：超时回调中确实移除了事件监听器，但如果 Promise 被提前 resolve（通过 `captureFrame`），监听器也会被正确移除。但如果视频加载失败且超时，则监听器会被清理，这是正确的。

**结论**：此问题实际上**没有 Bug**。超时和正常情况下都正确清理了监听器。

---

### 问题 2：缺少 AbortController 支持（高优先级）

**代码位置**：`useThumbnails.ts` 第 108-159 行

**问题描述**：`generateOne` 函数不支持 `AbortSignal`，无法在批量生成过程中取消单个缩略图的生成。

**影响**：当用户快速滚动时间线时，可能会启动大量不必要的缩略图生成任务。

**修复建议**：
```typescript
const generateOne = useCallback(
  async (
    video: HTMLVideoElement,
    canvas: HTMLCanvasElement,
    time: number,
    signal?: AbortSignal
  ): Promise<[string, string] | null> => {
    // ... 在检测到 signal.aborted 时提前返回
    if (signal?.aborted) return null;
  },
  []
);
```

---

### 问题 3：thumbCache 依赖问题（中优先级）

**代码位置**：`useThumbnails.ts` 第 161-264 行

**问题描述**：`thumbCache` 不在 `useEffect` 依赖数组中，而是通过 ref (`pendingRef`) 来跟踪待处理的缩略图。

**当前设计分析**：
- `thumbCache` 不在依赖中 → 避免每次缓存更新都重新执行生成逻辑
- 使用 `pendingRef` 来避免重复生成相同的缩略图
- 但这可能导致 `missingTimes` 计算不准确（基于过时的 `thumbCache`）

**影响**：轻微。`missingTimes` 计算时可能会包含已经在缓存中的缩略图，但由于 `pendingRef` 的存在，这些不会真正生成。

**修复建议**：可以接受当前设计，无需修改。

---

## 四、总结

### 缩略图功能状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 缩略图生成 | ✅ 正常 | 能正确生成缩略图 |
| 缩略图显示 | ✅ 正常 | Timeline 组件能正确显示缩略图 |
| 内存管理 | ✅ 正常 | DOM 元素在卸载时正确清理 |
| 事件清理 | ✅ 正常 | 事件监听器在使用后正确移除 |
| 取消机制 | ✅ 已修复 | 为 `generateOne` 添加 `AbortSignal` 支持 |

### 建议改进项

1. **中等优先级**：为 `generateOne` 函数添加 `AbortSignal` 支持 ✅ 已修复
2. **低优先级**：考虑使用 ref 替代 state 来存储 `thumbCache`，避免不必要的渲染
3. **低优先级**：添加进度取消功能（当用户取消生成时停止所有正在进行的任务）

---

## 缩略图功能修复记录

### 修复 1：添加 AbortSignal 支持

**时间**：2026-05-27
**问题**：批量生成缩略图时无法取消单个任务，导致用户快速滚动时产生大量不必要的任务
**修复内容**：
1. 为 `generateOne` 函数添加 `signal?: AbortSignal` 参数
2. 在函数开始时检查 `signal?.aborted` 状态
3. 添加 `onAbort` 事件监听器监听取消信号
4. 抽取 `cleanup` 函数统一处理清理逻辑（移除监听器、清除定时器）
5. 在 `captureFrame`、`timeout`、`onAbort` 中调用 `cleanup`

**修复前**：
```typescript
const generateOne = useCallback(
  async (video: HTMLVideoElement, canvas: HTMLCanvasElement, time: number): Promise<[string, string] | null> => {
    // ...
  }
);
```

**修复后**：
```typescript
const generateOne = useCallback(
  async (
    video: HTMLVideoElement,
    canvas: HTMLCanvasElement,
    time: number,
    signal?: AbortSignal
  ): Promise<[string, string] | null> => {
    // 检查是否已取消
    if (signal?.aborted) return null;
    
    return new Promise((resolve) => {
      // 监听取消信号
      const onAbort = () => {
        cleanup();
        resolve(null);
      };
      signal?.addEventListener("abort", onAbort);
      
      const cleanup = () => {
        clearTimeout(timeout);
        video.removeEventListener("seeked", onSeeked);
        video.removeEventListener("loadeddata", onLoadedData);
        video.removeEventListener("timeupdate", onTimeUpdate);
        signal?.removeEventListener("abort", onAbort);
      };
      // ...
    });
  }
);
```

**影响文件**：
- `src/hooks/useThumbnails.ts` - `generateOne` 函数