# 字幕样式同步架构分析

## 一、整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           App.tsx                                    │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ useProjectStorage() - 全局状态管理                            │    │
│  │   ├── sentences: Sentence[]                                  │    │
│  │   ├── videoName: string | null                              │    │
│  │   └── stylePresets: StylePreset[]                            │    │
│  │                                                               │    │
│  │ handleUpdateSentence(id, updates) → setSentences()            │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                              │                                       │
│           ┌──────────────────┼──────────────────┐                   │
│           ▼                  ▼                  ▼                   │
│  ┌─────────────────┐  ┌─────────────┐  ┌──────────────────┐        │
│  │  VideoPlayer    │  │ Timeline    │  │SentenceEditorModal│        │
│  │  (主视频字幕)    │  │            │  │  (弹窗字幕)       │        │
│  └─────────────────┘  └─────────────┘  └──────────────────┘        │
└─────────────────────────────────────────────────────────────────────┘
```

## 二、数据类型定义 (`types.ts`)

### SubtitleStyle 接口
```typescript
export interface SubtitleStyle {
  fontname: string;      // 字体名称
  fontsize: number;      // 字号
  color1: string;        // 主文字颜色 (hex)
  color3: string;        // 描边颜色
  color4: string;        // 阴影颜色
  bold: boolean;         // 粗体
  italic: boolean;        // 斜体
  underline: boolean;    // 下划线
  outline: number;       // 描边宽度 (0-10 px)
  shadow: number;        // 阴影宽度 (0-10 px)
  margin_l: number;      // 左边距
  margin_r: number;      // 右边距
  margin_v: number;      // 垂直边距
  offsetY: number;       // 垂直偏移
}
```

### Sentence 接口
```typescript
export interface Sentence {
  id: string;
  start: number;          // 开始时间
  end: number;            // 结束时间
  text: string;           // 遗留字段
  englishText: string;    // 英文字幕
  chineseText: string;    // 中文字幕
  style: {
    english: SubtitleStyle;   // 英文字幕样式
    chinese: SubtitleStyle;   // 中文字幕样式
  };
}
```

## 三、数据流分析

### 1. 主视频字幕渲染 (`VideoPlayer.tsx`)

**位置**: `VideoPlayer.tsx` 第 213-278 行

**关键逻辑**:
```typescript
// 根据当前播放时间查找对应句子
const displayTime = currentTime ?? videoRef.current?.currentTime ?? 0;
const currentSentence = sentences?.find(
  (s) => displayTime >= s.start && displayTime <= s.end
);

// 使用 Sentence 中的 style 渲染字幕
{currentSentence.chineseText && (
  <div className="video-subtitle video-subtitle-cn"
    style={{ ...buildSubtitleCSS(currentSentence.style.chinese) }}>
    {currentSentence.chineseText}
  </div>
)}
```

**问题**: 主视频使用 `currentSentence.style.chinese` 和 `currentSentence.style.english`，样式直接存储在 Sentence 对象中。

### 2. 弹窗字幕渲染 (`SentenceEditorModal.tsx` + `CSSSubtitleRenderer.tsx`)

**Modal 本地状态** (`SentenceEditorModal.tsx` 第 26-27 行):
```typescript
const [enStyle, setEnStyle] = useState<SubtitleStyle>({ ...sentence.style.english });
const [cnStyle, setCnStyle] = useState<SubtitleStyle>({ ...sentence.style.chinese });
```

**同步到全局** (`SentenceEditorModal.tsx` 第 59-62 行):
```typescript
// 保存样式变化到全局状态
useEffect(() => {
  onUpdate(sentence.id, { style: { english: enStyle, chinese: cnStyle } });
}, [sentence.id, enStyle, cnStyle, onUpdate]);
```

**样式面板** (`SubtitleStylePanel.tsx`):
- 提供 UI 控件修改 `enStyle` 和 `cnStyle`
- 通过 `updateStyle` 回调更新 Modal 的本地状态

**弹窗字幕渲染** (`CSSSubtitleRenderer.tsx`):
- 接收 `englishStyle` 和 `chineseStyle` props
- 使用 `useMemo` 计算样式
- `buildTextStyle()` 构建 CSS 样式对象

## 四、同步流程

### 样式修改 → 全局保存 → 主视频生效

```
用户修改样式
    │
    ▼
SubtitleStylePanel.updateStyle(updates)
    │
    ▼
SentenceEditorModal.setEnStyle/setCnStyle (本地状态)
    │
    ▼
useEffect 触发
    │
    ▼
onUpdate(sentence.id, { style: {...} })
    │
    ▼
App.handleUpdateSentence(id, updates)
    │
    ▼
setSentences(...) 更新全局状态
    │
    ▼
localStorage 保存
    │
    ▼
VideoPlayer 重新渲染 (因为 sentences prop 变化)
    │
    ▼
主视频字幕应用新样式 ✓
```

## 五、潜在问题分析

### 问题 1: CSSSubtitleRenderer 使用错误的属性名

**文件**: `CSSSubtitleRenderer.tsx` 第 2-18 行

**定义的接口** (与 types.ts 不一致):
```typescript
interface SubtitleStyle {
  fontFamily?: string;     // ❌ 错误: types.ts 使用 fontname
  fontSize?: number;       // ❌ 错误: types.ts 使用 fontsize
  color?: string;           // ❌ 错误: types.ts 使用 color1
  outlineColor?: string;    // ❌ 错误: types.ts 使用 color3
  outlineWidth?: number;    // ❌ 错误: types.ts 使用 outline
  shadowColor?: string;     // ❌ 错误: types.ts 使用 color4
  shadowWidth?: number;     // ❌ 错误: types.ts 使用 shadow
  // ...
}
```

**types.ts 实际定义**:
```typescript
export interface SubtitleStyle {
  fontname: string;        // ✓ 正确
  fontsize: number;         // ✓ 正确
  color1: string;          // ✓ 正确
  color3: string;          // ✓ 正确
  color4: string;          // ✓ 正确
  outline: number;         // ✓ 正确
  shadow: number;          // ✓ 正确
  // ...
}
```

**影响**: CSSSubtitleRenderer 定义的接口与实际传入的 props 类型不匹配，可能导致样式不生效。

### 问题 2: 属性映射缺失

**CSSSubtitleRenderer** 需要将传入的 `SubtitleStyle` 正确映射到 CSS 属性，但当前实现存在问题：

**当前 buildTextStyle 实现** (`CSSSubtitleRenderer.tsx` 第 83-108 行):
```typescript
function buildTextStyle(style: SubtitleStyle, isChinese: boolean): React.CSSProperties {
  const outlineW = style.outlineWidth ?? 4;      // ❌ 应为 style.outline
  const outlineC = style.outlineColor ?? '#000000'; // ❌ 应为 style.color3
  const shadowW = style.shadowWidth ?? 1;        // ❌ 应为 style.shadow
  const shadowC = style.shadowColor ?? '#000000'; // ❌ 应为 style.color4

  return {
    fontFamily: `"${style.fontFamily || 'Microsoft YaHei'}"`, // ❌ 应为 style.fontname
    fontSize: `${style.fontSize ?? 52}px`,                   // ❌ 应为 style.fontsize
    color: style.color ?? '#FFFFFF',                          // ❌ 应为 style.color1
    // ...
  };
}
```

### 问题 3: 弹窗字幕与主视频字幕可能不同步

由于:
1. 弹窗使用本地状态 `enStyle`/`cnStyle`
2. 主视频使用全局 `sentences[i].style`
3. 同步依赖 `useEffect` 触发 `onUpdate`

**可能导致**: 用户在弹窗中修改样式后，如果 `onUpdate` 未正确调用或 `setSentences` 未正确更新，主视频字幕不会实时反映更改。

## 六、修复建议

### 1. 统一 SubtitleStyle 接口

**CSSSubtitleRenderer.tsx** 应导入 `types.ts` 中的 `SubtitleStyle`:
```typescript
import type { SubtitleStyle } from '../types';  // 替换本地接口定义
```

### 2. 修复属性映射

**buildTextStyle 函数** 应使用正确的属性名:
```typescript
function buildTextStyle(style: SubtitleStyle, isChinese: boolean): React.CSSProperties {
  const outlineW = style.outline ?? 2;
  const outlineC = style.color3 ?? "#000000";
  const shadowW = style.shadow ?? 1;
  const shadowC = style.color4 ?? "#000000";

  return {
    fontFamily: `"${style.fontname || 'Arial'}", sans-serif`,
    fontSize: `${style.fontsize ?? 48}px`,
    color: style.color1 || '#FFFFFF',
    // ...
  };
}
```

### 3. 确保同步机制健壮

当前同步依赖:
- `SentenceEditorModal` 的 `useEffect` (第 59-62 行)
- `onUpdate` 回调传递到 `App.handleUpdateSentence`
- `setSentences` 更新全局状态

**建议**: 验证这个链条的每个环节都正常工作。

## 七、文件清单

| 文件 | 职责 |
|------|------|
| `src/types.ts` | 定义 SubtitleStyle, Sentence 等核心类型 |
| `src/hooks/useLocalStorage.ts` | useProjectStorage 管理全局状态持久化 |
| `src/App.tsx` | 根组件，管理 sentences 状态 |
| `src/components/VideoPlayer.tsx` | 主视频播放器 + 字幕渲染 |
| `src/components/SentenceEditorModal.tsx` | 字幕编辑弹窗 |
| `src/components/CSSSubtitleRenderer.tsx` | 弹窗字幕渲染组件 |
| `src/components/SubtitleStylePanel.tsx` | 样式编辑面板 |

## 八、测试建议

1. **样式修改测试**: 在弹窗中修改字幕样式，验证主视频字幕是否同步更新
2. **属性覆盖测试**: 逐个修改字体、字号、颜色、描边、阴影，验证每个属性是否正确应用
3. **持久化测试**: 修改样式后刷新页面，验证样式是否从 localStorage 正确恢复
4. **边界测试**: 修改极值（字号 20/120、描边 0/10 等），验证是否正常工作
